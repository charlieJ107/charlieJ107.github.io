name: Retired reverse content synchronization

# Kept as a visible explanation for anyone looking for the old manual action.
# Public pushes and pull requests can no longer modify the private repository.
on:
  workflow_dispatch:

permissions: {}

jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Edit the private source repository. Published content now synchronizes in one direction to this repository.'
