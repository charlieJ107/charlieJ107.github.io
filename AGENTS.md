# 公开内容仓库指南

## 项目与边界

这是个人网站的公开内容镜像，保存博客、简历、个人项目介绍等已发布内容和发布历史。内容在私有主仓库编辑，经统一过滤后单向同步到这里；本仓库不再作为 Git 子模块或私有源码的构建环境。

- 默认使用中文沟通；正文保留原语言、作者语气和技术含义。
- 本文件适用于本仓库；开始前检查 Git 状态和相关内容，保留用户已有修改。
- 本仓库接收发布内容及其配图。日常文章修改应在私有主仓进行，再由发布流程同步；避免直接编辑自动管理的文件。
- 仓库内容及历史可公开访问。不要加入凭据、私有源码或未获准公开的个人资料。
- 不编造个人经历、任职日期、成果数据、项目状态或联系方式；缺失事实应向用户确认。

## 内容入口与格式

| 路径 | 用途 |
| --- | --- |
| `blog/en/`、`blog/zh/` | 英文、中文博客 |
| `cv/en/`、`cv/zh/` | 按类别组织的简历条目 |
| `projects/en/`、`projects/zh/` | 当前源码加载的个人项目内容 |
| `service/`、`about/` | 服务介绍、关于页面内容 |
| `images/` | 内容图片资源 |
| `template/` | 写作参考模板，使用前核对 schema |
| `.legacy-pages/` | 自动生成的旧域迁移页面 |
| `.public-content-manifest.json` | 自动同步管理的文件清单 |
| `.github/workflows/` | 只部署迁移页面的 GitHub Pages 流程 |

- 以源码仓库 `src/content.config.ts` 和 `src/schemas/` 为格式依据；本仓库单独打开时若源码不可访问，参考相邻有效内容并说明无法核验的字段。
- 当前加载器匹配 `.md`。使用 Markdown 和 YAML frontmatter；不要假定 `.mdx` 已受支持。
- `project/` 和 `projects/` 同时存在，但当前项目集合读取 `projects/`。不要向错误目录新增内容，也不要未经任务要求批量迁移旧目录。
- `home/`、`store/` 虽存在，目前未在源码中注册为内容集合；不能把文件存在当作页面已接入。
- 博客必填 `title`、`description`、`date`；项目必填 `title`、`updatedAt`；简历必填 `type`、`title`、`startDate`；服务必填 `title`。完整约束以对应 schema 为准。
- 此处保留的旧模板仅作历史参考；新内容使用私有主仓的模板和发布规则。
- 保留真实发布日期，日期使用无歧义格式（如 `YYYY-MM-DD`）。修改可选字段时检查页面是否支持其值和类型。
- 图片路径按当前文章位置和现有用法解析，验证资源存在，补充有意义的替代文本。
- 文件名可能影响条目 ID 和公开 URL。避免无关重命名，调整路径时检查链接及源码路由。
- 中英文内容按现有目录组织；只在任务需要时补充翻译，保持两种语言中的事实一致。
- 只有私有主仓中明确 `draft: false` 的内容才导出。不要在公开仓库保存草稿；撤回当前发布文件不会抹除其公开 Git 历史。

## 验证与跨仓库协作

- 本仓库没有 `package.json`，不要在这里执行 npm 安装、构建或测试。
- 修改内容后检查 frontmatter、Markdown、图片、内部链接和事实准确性；涉及渲染的改动在源码仓库验证。
- 同级私有主仓与本仓库是不同工作目录；网站和镜像都应来自主仓同一次内容准备结果，不进行双向同步。
- 在具备源码与待验证内容的环境中，按源码指南安装依赖并运行 `npm run build`，预览受影响页面；只有内容仓库访问权限时，明确报告未完成构建验证。
- 仅修改本指南等开发文档时检查路径、命令和 diff 即可。
- 交付时说明内容变更、验证结果，以及是否需要协调源码或更新子模块引用。

## 发布注意事项

`github-pages.yml` 只把 `.legacy-pages/` 部署到 GitHub Pages；没有迁移产物时保留当前线上部署。旧的 `update-source.yml` 已停用，不再更新私有仓库或触发主站构建。

推送迁移页面或运行发布工作流可能改变旧网址入口，按任务已有授权执行。保留 Git 历史和非自动管理的文件；同步脚本通过清单管理新增、修改与撤回。此处不得配置访问私有源码的凭据。
