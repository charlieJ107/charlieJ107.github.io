# 仓库指南

## 项目与边界

本仓库保存 [zhuoling.space](https://zhuoling.space) 已发布的内容：博客、简历、个人项目与服务介绍的 Markdown 原文及配图，并承担网址 `charliej107.github.io` 的跳转。仓库内容和 Git 历史完全公开。

- 使用与用户一致的沟通；正文保留原语言、作者语气和技术含义。
- 开始前检查 Git 状态和相关内容，保留用户已有修改。
- 内容文件和 `.legacy-pages/` 由网站发布流程自动写入，会被下一次发布覆盖。不要把它们当作手工维护的文件来改动；确有内容问题时，向用户说明应在发布流程中修正。
- 不要加入凭据、访问令牌或未获准公开的个人资料。撤回文件不会抹除其已公开的 Git 历史。
- 不编造个人经历、任职日期、成果数据、项目状态或联系方式；缺失事实应向用户确认。

## 内容入口与格式

| 路径 | 用途 |
| --- | --- |
| `blog/en/`、`blog/zh/` | 英文、中文博客 |
| `cv/en/`、`cv/zh/` | 按类别组织的简历条目 |
| `projects/en/`、`projects/zh/` | 个人项目介绍 |
| `service/`、`about/` | 服务介绍、关于页面内容 |
| `images/` | 内容图片资源 |
| `.legacy-pages/` | 自动生成的旧网址跳转页面 |
| `.public-content-manifest.json` | 自动同步管理的文件清单 |
| `.github/workflows/` | 只部署跳转页面的 GitHub Pages 流程 |

- 使用 Markdown 和 YAML frontmatter，当前内容加载只匹配 `.md`，不要假定 `.mdx` 已受支持。
- 博客必填 `title`、`description`、`date`；项目必填 `title`、`updatedAt`；简历必填 `type`、`title`、`startDate`；服务必填 `title`。在本仓库无法核验完整 schema 时，参考相邻有效内容，并说明哪些字段未经核验。
- 保留真实发布日期，日期使用无歧义格式（如 `YYYY-MM-DD`）。
- 图片路径按当前文章位置和现有用法解析，验证资源存在，补充有意义的替代文本。
- 文件名决定条目 ID 和公开 URL。避免无关重命名；调整路径时检查站内链接。
- 中英文内容按现有目录组织；只在任务需要时补充翻译，保持两种语言中的事实一致。
- 只有明确 `draft: false` 的内容才会发布到这里，不要在本仓库保存草稿。

## 验证

- 本仓库没有 `package.json`，不要在这里执行 npm 安装、构建或测试。
- 修改内容后检查 frontmatter、Markdown、图片、内部链接和事实准确性；只有本仓库访问权限时，明确报告未完成渲染验证。
- 仅修改本指南等文档时，检查路径、命令和 diff 即可。

## 发布注意事项

[`github-pages.yml`](.github/workflows/github-pages.yml) 只把 `.legacy-pages/` 部署到 GitHub Pages；没有跳转页产物时保留当前线上部署。跳转页按网站实际路由自动生成，每页返回 200 并指向对应新地址，`404.html` 兜底未知路径。

推送跳转页或运行发布工作流可能改变旧网址入口，按任务已有授权执行。保留 Git 历史和非自动管理的文件。
